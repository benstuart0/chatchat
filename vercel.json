{
  "buildCommand": "cd client && npm install --no-save && npm run build",
  "outputDirectory": "client/.next",
  "installCommand": "cd client && npm install --no-save",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
} 